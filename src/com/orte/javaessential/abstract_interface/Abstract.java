package com.orte.javaessential.abstract_interface;

public abstract class Abstract {
    // object can't be created
    // abstract can be class and method
    // if only one method abstract then all class is abstract too
    // abstract class can consist of all non-abstract members but it has no sense
    // implementation of abstract method is overriding
}

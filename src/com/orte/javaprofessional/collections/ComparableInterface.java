package com.orte.javaprofessional.collections;

public class ComparableInterface {
    // java.lang
    // one method compareTo(Object obj)
    // used to sort ordered lists or arrays of objects
    // compares call object with object and returns
    // 0 if objects equal
    // negative if object less then parameter
    // positive if object more then parameter
    // Used in Arrays.sort();
    // objects of user classes must implement comparable interface to get sorted.
}

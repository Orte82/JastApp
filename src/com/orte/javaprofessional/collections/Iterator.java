package com.orte.javaprofessional.collections;

public class Iterator {
    // Iterator interface makes available access to collection elements and iteration over them

    // ListIterator => improved iterator for processing lists
    // ListIterator allows insert and remove elements and iterates in both ways it list

    // boolean hasNext()
    // E next()
    // void remove() => removes last element.next() called

    // ListIterator has additionally

    // E previous(), int previousIndex(), boolean hasPrevious()
    // intNextIndex() return number of next element

}

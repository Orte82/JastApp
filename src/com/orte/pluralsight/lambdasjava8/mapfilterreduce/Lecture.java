package com.orte.pluralsight.lambdasjava8.mapfilterreduce;

public class Lecture {
    public static void main(String[] args) {
        // Java 7
        // List<Person> people = ...;
        // fictional class Lists
        // List<Integer> ages = Lists.map(people, person -> person.getAge());
        // List<Integer> ageGT20 = Lists.filter(ages, age -> age > 20);
        // int sum = Lists.reduce(agesGT20, (a1, a2) -> a1 + a2);
        // associativity
        // BinaryOperator<Integer> op1 = (i1, i2) -> i1 + i2;
        // Reduction step implementation
        // List<Integer> ints = new ArrayList<>();
        // int sum = 0;
        // BinaryOperator<Integer> op = (i1, i2) -> i1 + i2;
        // for(int i : ints){
        //          sum = op.apply(sum, i);
        // }
        // Optional wrapper class that can be empty

    }
}

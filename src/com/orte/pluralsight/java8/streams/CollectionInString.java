package com.orte.pluralsight.java8.streams;

public class CollectionInString {
    public static void main(String[] args) {
        // List<Person> persons = ...;
        // String result =
        // persons.stream()
        //        .filter(person -> person.getAge() > 20)
        //        .map(Person::getLastName)
        //        .collect(Collectors.joining(", "))
//collect to list// .collect(Collectors.toList()); // List<String> list = ^
    }


    //Collecting in Map
    // Map<Integer, List<Person>> result = persons.stream()
    //                                            .filter(person -> person.getAge() > 20)
    //                                            .collect(
    //                                                      Collectors.groupingBy(Person::getAge)
    //                                                      );
    //                                             Collectors.counting()//Downstream collector counts number of people of each age

}
